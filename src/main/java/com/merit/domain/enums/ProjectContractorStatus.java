package com.merit.domain.enums;

public enum ProjectContractorStatus {
    // * these depend on whether the company click the accept or reject button in project details
    ACCEPTED, REJECTED, PENDING
}
